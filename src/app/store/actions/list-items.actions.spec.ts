import { ListItems.Actions.Ts } from './list-items.actions.ts';

describe('ListItems.Actions.Ts', () => {
  it('should create an instance', () => {
    expect(new ListItems.Actions.Ts()).toBeTruthy();
  });
});
