import { ListItems.State.Ts } from './list-items.state.ts';

describe('ListItems.State.Ts', () => {
  it('should create an instance', () => {
    expect(new ListItems.State.Ts()).toBeTruthy();
  });
});
